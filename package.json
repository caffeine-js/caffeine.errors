{
  "name": "@caffeine/errors",
  "author": {
    "name": "Alan Reis",
    "email": "alanreisanjo@gmail.com",
    "url": "https://hoyasumii.dev"
  },
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "type": "module",
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@types/bun": "latest",
    "@vitest/coverage-v8": "^4.0.18",
    "tsup": "^8.5.1",
    "vite-tsconfig-paths": "^6.0.5",
    "vitest": "^4.0.18"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "build": "tsup src --format cjs,esm --dts --tsconfig tsconfig.build.json --clean",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "prepare": "bun run build && bun link"
  }
}